from django.urls import path
from .views import RegistroView

urlpatterns = [
    # Aquí puedes añadir las rutas para tu API de usuarios
    # path('registro/', RegistroView.as_view(), name='registro'),
]